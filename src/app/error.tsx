"use client";

// this takes cares of errors at global level,
// if there is no `ErrorBoundary` this takes care of.
const ErrorPage = () => {
  return <div>Global Error</div>;
};

export default ErrorPage;
